import styles from "./VuesaxOutlineArrowDown.module.css";

export const VuesaxOutlineArrowDown = ({ style }: { style: any }) => {
  return (
    <div style={style} className={styles.vuesaxOutlineArrowDown0}>
      <div />
    </div>
  );
};
