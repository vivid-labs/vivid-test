import { Navigation } from "./Navigation";

export const NavigationPage = ({ style }: { style?: any }) => {
  return (
    <Navigation
      style={{
        height: "fit-content",
        width: "100%",
      }}
    />
  );
};
