import { CreateAccountSection } from "./CreateAccountSection";

export const CreateAccountSectionPage = ({ style }: { style?: any }) => {
  return (
    <CreateAccountSection
      screen="Desktop"
      style={{
        height: "fit-content",
        width: "1280px",
      }}
    />
  );
};
