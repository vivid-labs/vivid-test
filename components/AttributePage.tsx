import styles from "./AttributePage.module.css";

export const AttributePage = ({ style }: { style?: any }) => {
  return (
    <div style={style} className={styles.attributePage}>
      <div className={styles.frame_3} />
    </div>
  );
};
