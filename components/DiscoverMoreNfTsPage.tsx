import { DiscoverMoreNfTs } from "./DiscoverMoreNfTs";

export const DiscoverMoreNfTsPage = ({ style }: { style?: any }) => {
  return (
    <DiscoverMoreNfTs
      screen="Desktop"
      style={{
        height: "fit-content",
        width: "100%",
      }}
    />
  );
};
