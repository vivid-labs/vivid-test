import { HeroSection } from "./HeroSection";
export const HeroSectionPage = ({ override }: { override?: any }) => {
  return (
    <HeroSection
      override={{
        width: "100%",
      }}
      figma-id="4:6:880"
    />
  );
};
